---
const { title = "Nexus Coach", description = "Klarheit. Fokus. Entwicklung." } = Astro.props;
---

<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>{title}</title>
    <meta name="description" content={description} />

    <!-- KEIN Astro.head hier -->

    <link rel="manifest" href="/manifest.json" />
    <meta name="theme-color" content="#0d0d0d" />
    <link rel="icon" href="/icons/icon-192.png" />

    <style>
      body {
        margin: 0;
        background: #0d0d0d;
        color: #f5f5f5;
        font-family: system-ui, sans-serif;
      }
      main {
        padding: 1.5rem;
        max-width: 900px;
        margin: 0 auto;
      }
    </style>
  </head>

  <body>
  ---
  import Menu from "../components/Menu.astro";
  const { title = "Nexus Coach", description = "Klarheit. Fokus. Entwicklung." } = Astro.props;
  ---
  
  <!DOCTYPE html>
  <html lang="de">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1" />
  
      <title>{title}</title>
      <meta name="description" content={description} />
  
      <link rel="manifest" href="/manifest.json" />
      <meta name="theme-color" content="#0d0d0d" />
      <link rel="icon" href="/icons/icon-192.png" />
  
      <style>
        body {
          margin: 0;
          background: #0d0d0d;
          color: #f5f5f5;
          font-family: system-ui, sans-serif;
        }
        main {
          padding: 1.5rem;
          max-width: 900px;
          margin: 0 auto;
        }
      </style>
    </head>
  
    <body>
      <Menu />
      <main>
        <slot />
      </main>
    </body>
  </html>
  
    </head>
  
    <body>
      <Menu />
      <main>
        <slot />
      </main>
    </body>
  </html>
  
    <main>
      <slot />
    </main>
  </body>
</html>
